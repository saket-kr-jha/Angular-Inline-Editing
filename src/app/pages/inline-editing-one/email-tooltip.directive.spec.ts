import { EmailTooltipDirective } from './email-tooltip.directive';

describe('EmailTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
